export const usersSelector = ({users}) => users.users;

export const sizeSelector = ({users}) => users.size;

export const totalSelector = ({users}) => users.total;

export const pageSelector = ({users}) => users.page;

export const fetchingSelector = ({users}) => users.fetching;

export const followingSelector = ({users}) => users.following;

export const availableSelector = ({users}) => users.available;